{
  "name": "tests",
  "private": true,
  "scripts": {
    "test": "jest --runInBand --forceExit",
    "test:concurrency": "jest concurrency.spec.ts --forceExit",
    "test:hold": "jest hold-expiry.spec.ts --forceExit",
    "test:idempotency": "jest idempotency.spec.ts --forceExit",
    "test:leave": "jest leave-conflict.spec.ts --forceExit",
    "test:llm": "jest llm-failure.spec.ts --forceExit",
    "test:urgency": "jest urgency-floor.spec.ts --forceExit",
    "test:dedupe": "jest dedupe.spec.ts --forceExit"
  },
  "dependencies": {
    "@healthcare/contracts": "workspace:*",
    "@healthcare/db": "workspace:*",
    "@healthcare/prompts": "workspace:*",
    "@prisma/client": "^6.8.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.7.0",
    "uuid": "^11.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testTimeout": 60000,
    "roots": ["<rootDir>"]
  }
}
